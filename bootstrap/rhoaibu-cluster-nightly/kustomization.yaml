apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../gitops-operator    # GitOps operator subscription
  - ../argocd-instance    # ArgoCD instance config
  - cluster-config-app.yaml  # Root Application that manages cluster overlay
